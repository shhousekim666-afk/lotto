{
  "name": "lotto-recommendation",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'",
    "test:browser": "node tests/browser-smoke.js",
    "validate": "node backtest/validate-data.js && node backtest/validate-results.js"
  },
  "devDependencies": { "@playwright/test": "1.58.2" }
}
